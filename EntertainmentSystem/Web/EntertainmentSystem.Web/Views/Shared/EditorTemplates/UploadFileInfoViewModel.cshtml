@using EntertainmentSystem.Web.ViewModels.Upload
@model UploadFileInfoViewModel

<br />
<div class="form-group">
    @Html.LabelFor(m => m.Title)
    @Html.EditorFor(m => m.Title)
</div>

<div class="form-group">
    @Html.LabelFor(m => m.Description)
    @Html.TextAreaFor(m => m.Description, new { @class = "form-control" })
</div>

<div class="form-group">
    <label class="control-label" for="file-upload-category">Category</label>
    <select class="form-control" id="file-upload-category">
        <option value="" selected="selected" disabled="disabled" style="display:none;">Select category</option>
        @*@if (Model != null)
            {
                if (Model.Count() > 0)
                {
                    foreach (var category in Model)
                    {
                        <option value="@category.Id">@category.Name</option>
                    }
                }
            }*@
    </select>
</div>

<div class="form-group">
    <label class="control-label" for="file-upload-collection">Collection</label>
    <select class="form-control" id="file-upload-collection">
        <option value="">Nothing selected</option>
        @*@if (Model != null)
            {
                if (Model.Count() > 0)
                {
                    foreach (var collection in Model)
                    {
                        <option  value="@collection.Id">@collection.Name</option>
                    }
                }
            }*@
    </select>
</div>
